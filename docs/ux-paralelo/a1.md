# A1: registro e handoff

## Onde parei

5.3 a 5.6 feitas e mergeadas na `main` (merge parcial da Fase 5, a pedido do
Gabriel, que precisou sair). **Falta da minha parte:**

- Fase 5: **5.7** (webhook de entrada com teste e status, talvez migration nova:
  número pelo diretório + `origin/main`, só local) e **5.8** (abas de
  Automações no celular). Depois, merge de fim da Fase 5.
- Fase 6 inteira (6.1 a 6.7), com merge ao fim.
- Pendências vistas: editor no celular rola de lado (painel direito fora da
  tela), não é tarefa minha no plano; o Duplicar do menu ⋯ da lista não abre o
  editor, então não mostra "Antes de publicar".
- Banco: nenhuma migration até aqui. Nada a aplicar em produção.
- Scripts de print em `.ux-local/` (ignorado): `importado.mjs`,
  `compartilhar.mjs`, `catalogo.mjs`, `ia-http.mjs`, `historico.mjs`; todos
  criam dado temporário na conta de revisão e apagam no fim.

## Registro

- 23/09, tarefa 5.3: `antesDePublicar` (`src/core/flow/antes-de-publicar.ts`)
  monta o checklist em cima dos problemas do `validar()`, então resolver no
  bloco tira o item na hora. Aba "Antes de publicar" no painel do editor, só com
  `?origem=importado|duplicado` (importar JSON, importar por link e Duplicar do
  diálogo "Nova automação"); o Duplicar do menu ⋯ continua na lista, sem abrir o
  editor. O editor passou a mandar etapas e etiquetas ao `validar()` (antes só o
  servidor conferia). Achado corrigido junto: os blocos Etiqueta e Anotação não
  tinham desenho em `tiposDeNo` e apareciam como caixa branca vazia. Link
  compartilhado: versão e validade em cima do endereço ("expirou em",
  "foi revogado em"), e a página pública diz a data. Editor no celular continua
  rolando de lado (painel fora da tela), como antes; não é desta tarefa.
- 23/09, tarefa 5.4: grupos e busca moram em `src/core/flow/blocos.ts`
  (`GRUPOS_DE_BLOCOS`, `filtrarCatalogo`); "Pesquisa de satisfação" (NPS) ficou
  em Conversar e "Falar com humano" em Integrar, como no plano. Enter na busca
  com um resultado só põe o bloco. "Etapa do quadro" virou "Etapa do funil" no
  catálogo, no painel, na prévia pública do link e no texto de Sequências.
- 23/09, tarefa 5.5: `validar()` ganhou `URL_INVALIDA` e `URL_INTERNA`
  (endereço que não se lê como link, sem domínio, localhost, IP privado); o
  painel HTTP mostra o mesmo erro embaixo do campo. Isso passa a recusar
  publicar `https://` sozinho, e o preset "O meu próprio sistema" nasce assim
  (teste do preset agora preenche o endereço). IA: grupos "Só consulta" e
  "Grava dados", loja com selo "só consulta", e cada consulta que grava mostra
  a política desta conta lida de `lerPoliticas` (`src/core/autonomia-da-ia.ts`
  traduz). **Desvio:** a política `humano` aparece como "pede confirmação",
  porque o motor hoje trata tudo que não é `automatico` igual
  (`resolver.ts:799`); não existe aprovação pela equipe. Sem print de antes dos
  painéis (só o do editor fechado).
- 23/09, tarefa 5.6: `compararGrafos` (`src/core/flow/comparar.ts`) compara
  por id de bloco, ignora posição e acusa ligação mudada à parte. Histórico:
  "Ver" abre o desenho da versão só leitura (`GrafoDeLeitura`, com
  `ReactFlowProvider` próprio para não dividir estado com o editor), pintando
  o que volta (verde) e o que muda (âmbar), e lista "Volta / Sai / Muda" em
  relação ao rascunho. A confirmação diz "vira a versão vN+1; só as conversas
  que começarem agora usam". O desenho da versão vem por
  `acaoGrafoDaVersao` ao clicar (confere conta e fluxo).
- 23/09, sessão de fechamento (um agente só, worktree `../autofluxos-ux`,
  branch `ux/restante`, porta 3101), tarefa 5.7: endereço com "Copiar" em
  cima do cadastro; cada webhook diz "Nunca chamado", "Autenticada em 23/09 às
  18:56" ou "Assinatura inválida em ..." (`estadoDoWebhook` em
  `src/core/webhook-de-entrada.ts`, com teste; a data mais nova ganha).
  **Migration `0095_webhook_recusado.sql`** (coluna `recusada_em`), aplicada
  só no local; **produção pendente**. A recusa não diz qual webhook era, então
  `marcarRecusa` marca todos os ativos da conta, depois da resposta e dentro do
  limite por minuto. Apagar explica que o outro sistema passa a ser recusado e
  que não dá para desfazer. Provado com chamada real na rota local (200 e 401).
- 23/09, tarefa 5.8: em 390 px as abas já cabiam; em 320 px quebravam em duas
  linhas (Sequências e Campanhas desciam). Abas e sub-abas viram faixa que rola
  de lado, sem quebrar. O defeito maior era outro: a barra de seções do celular
  (Painel, Inbox, Atividades...) cortava o item aceso fora da tela em
  Automações e Configurações. `BarraLateral` rola até ele ao abrir e mostra um
  esmaecido na borda direita enquanto houver mais itens. Nome da conta no
  cabeçalho do celular encolhe abaixo de 375 px para o avatar caber.
  `prints.mjs` aceita `LARGURAS='[[320,700,"-320"]]'`.
- 23/09, tarefa 6.1: `enviadasHojePelaConta` soma o que saiu hoje (dia de
  Brasília, retida conta) e o que está na fila de transmissão agendada ou
  enviando que sai até o fim do dia; cancelada não conta. A ação recalcula no
  servidor e ignora o que a tela mandar. **Desvio:** o veredito antigo deixava
  passar lista maior que o limite dizendo "as outras saem nos próximos dias",
  mas o motor manda tudo de uma vez e não fatia por dia; agora barra com os
  números ("Hoje já saíram 180 de 250. Esta lista tem 120."). Transmissão
  marcada para outro dia não pesa no consumo de hoje (`saiNoDiaDeHoje`). Sem
  print de antes do diálogo (antes ele não mostrava número nenhum).
- 23/09, tarefa 6.2: **migration `0096_progresso_das_transmissoes.sql`**
  (função `progresso_das_transmissoes(uuid[])`, `security invoker`, só
  `service_role`), aplicada só no local; **produção pendente**. Sem ela,
  `progressoDas` cai na leitura antiga uma a uma (a tela não cai). A leitura
  antiga também mentia acima de 1.000 destinatários (`max_rows` do PostgREST).
  A troca na página entrou no commit da 6.1, junto do repositório.
- 23/09, tarefa 6.3: filtros Estado (inclui "Com mensagem retida" e "Com
  falha", lidos do progresso) e Período na `BarraDeLista`, com busca pelo
  nome; regras puras em `src/core/transmissoes-na-tela.ts`. Rota nova
  `transmissoes/[transmissaoId]`: números, "Por que parou", "E agora" e
  destinatários paginados (50) com filtro por estado no endereço, telefone
  legível, motivo da falha e link para o contato. A próxima ação lê o motivo
  gravado pelo motor: modelo pausado leva aos modelos e diz que as retidas não
  saem; número desconectado leva ao WhatsApp; o resto diz que não há nova
  tentativa automática. `ROTULO_DO_ESTADO`, `Numeros` e `ProximaAcao` saíram
  para `numeros.tsx` (o detalhe é de servidor e não lê constante de módulo de
  cliente). Exportar falhas em arquivo não entrou (não está no plano).
- 23/09, tarefa 6.4: `estadoDaConexao` e `seloDaConexao` em
  `src/core/conexoes.ts`; componente `CamadasDaConexao` (cadastro,
  autorização, último evento, falha com a ação) no topo de WhatsApp,
  Instagram e Anúncios. `catalogoDeIntegracoes`
  (`src/server/catalogo-de-integracoes.ts`) é a lista única de Integrações e
  do "N de M" do índice (C02): o Telegram fica fora do total, a Loja Magento
  passou a contar. No Início, `falhaDoCanal` usa `estadoDaConexao` (pendência
  do A3) e o selo "Atendendo agora" virou "Configurado" com "Última mensagem
  recebida há 2 h". **Desvios:** o último evento é **da conta**, não do
  número: `messages` não guarda o canal e a sessão só existe com automação,
  então a tela diz "na conta". A inscrição da página de anúncio no webhook não
  é gravada em lugar nenhum; a função trata `webhookInscrito: false` como
  falha (com teste), mas as telas passam `null` (desconhecido). Achado
  corrigido junto: a tela do WhatsApp listava a conta do Instagram como número
  "sem número".
- 23/09, tarefa 6.5: cada cartão de Integrações termina com a ação escrita
  ("Conectar ›", "Configurar ›", a próxima ação quando há falha ou token
  vencendo, "Em breve" sem seta no Telegram). Selo de quem não está ligado
  virou "não conectada" (antes repetia "conectar"). Categorias Canal,
  Anúncios, API, Loja e o texto do Telegram vieram na 6.4, pelo catálogo.
- 23/09, tarefa 6.6: o cartão de cada número tem três blocos: **Estado da
  conexão** (conectado, trazendo conversas com barra, importação parada, caiu;
  "Reconectar" só quando caiu ou travou), **O que o bot responde** ("Responde
  em N de 4 situações", `respostasDoNumero` em `core/papeis-do-numero.ts` com
  teste, cada situação calada com o motivo e "Publicar ›" quando o fluxo está
  em rascunho) e **Webhook** (endereço para copiar e a última mensagem da
  conta). A seção de webhook separada no fim da tela saiu. **Desvio:** o
  desembarque gravado no canal passa a valer no bloco mesmo sem estado de
  coexistência, para o topo (6.4) e o cartão não se contradizerem.
