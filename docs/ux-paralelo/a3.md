# A3: registro e handoff

## Onde parei

Fase 9 (9.1) feita na branch `ux/a3-inicio-configuracoes`; próximo passo é o
merge da Fase 9 na `main`, depois 10.2 a 10.5 (10.1 espera a Fase 7 na `main`).

## Registro

- 23/09, tarefa 9.1: regra pura `pendenciasDoInicio`
  (`src/core/pendencias-do-inicio.ts`, com teste). "Precisa de você" lista
  falha de conexão, conversas que pediram pessoa, esperando resposta,
  atividades vencidas e de hoje, cada linha levando à lista filtrada; embaixo,
  "Na fila há mais tempo" com as conversas como antes. "Ninguém esperando" só
  com tudo zero. Desvios: (1) **conexão com falha** usa só o que já existe
  (`desembarcadoEm` do WhatsApp e token do Instagram vencido), porque a
  `estadoDaConexao` da 6.4 é do A1 e ainda não está na `main`; quando entrar,
  trocar `falhaDoCanal` por ela. (2) **Conversas não são recortadas por
  escopo**: o Inbox também não recorta, e o número precisa bater com o que o
  link abre; quem não tem `atender` não vê conversa nem agenda. Conexão só para
  quem configura. (3) Passo 3: o cartão de onboarding já sumia com o
  onboarding concluído; sem onboarding começado, "Deixe o sistema com a sua
  cara / Personalizar sistema" virou "Defina o objetivo da conta / Objetivo e
  recursos". (4) Passo 4 sem mudança: Relatórios ainda não existe.
  Print local: 60 vencidas + 7 hoje = 67, igual ao número do menu.
