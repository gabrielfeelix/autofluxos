# A3: registro e handoff

## Onde parei

Fase 9 na `main` (`673ba36`, deploy READY). 10.2, 10.3 e 10.4 (menos o Passo 3) feitas na branch. Próxima: 10.5.
Esperam a Fase 7 na `main`: 10.1 e 10.4 Passo 3 (rodapé, que a 7.5 também muda). 10.6 bloqueada (Brevo).

## Registro

- 23/09, tarefa 9.1: regra pura `pendenciasDoInicio`
  (`src/core/pendencias-do-inicio.ts`, com teste). "Precisa de você" lista
  falha de conexão, conversas que pediram pessoa, esperando resposta,
  atividades vencidas e de hoje, cada linha levando à lista filtrada; embaixo,
  "Na fila há mais tempo" com as conversas como antes. "Ninguém esperando" só
  com tudo zero. Desvios: (1) **conexão com falha** usa só o que já existe
  (`desembarcadoEm` do WhatsApp e token do Instagram vencido), porque a
  `estadoDaConexao` da 6.4 é do A1 e ainda não está na `main`; quando entrar,
  trocar `falhaDoCanal` por ela. (2) **Conversas não são recortadas por
  escopo**: o Inbox também não recorta, e o número precisa bater com o que o
  link abre; quem não tem `atender` não vê conversa nem agenda. Conexão só para
  quem configura. (3) Passo 3: o cartão de onboarding já sumia com o
  onboarding concluído; sem onboarding começado, "Deixe o sistema com a sua
  cara / Personalizar sistema" virou "Defina o objetivo da conta / Objetivo e
  recursos". (4) Passo 4 sem mudança: Relatórios ainda não existe.
  Print local: 60 vencidas + 7 hoje = 67, igual ao número do menu.
- 23/09, merge da Fase 9 na `main`: `673ba36`, deploy READY. Nenhum commit
  dos outros agentes tinha entrado desde `f85d71c`.
- 23/09, tarefa 10.6 **bloqueada**: a Brevo só tem `verandi.mail.4yu.com.br`
  autenticado (API `/v3/senders/domains`), e `autofluxos.mail.4yu.com.br` não
  tem TXT nem DKIM no DNS (consulta em dns.google). Falta: criar o domínio na
  Brevo, publicar TXT de posse, SPF e DKIM na zona da Hostinger, esperar
  autenticar. Até lá, cadastro com senha continua.
- 23/09, tarefa 10.2: regra pura `trilhaDeConfiguracao` e `automacaoNoAr`
  (`src/core/trilha-de-configuracao.ts`, com teste). "Não duplicar a regra":
  o cálculo de canal atendendo por automação publicada saiu do Início e os dois
  usam `automacaoNoAr`. Os passos da trilha não são os de `primeiros-passos`
  (o plano pede empresa e horário, que o Início não cobra), então só os fatos
  são comuns. Desvio: "Testar" só conta feito com a automação no ar; contato
  importado não prova que o canal responde.
- 23/09, tarefa 10.3: `FormularioSalvar` mantém "Salvo" até a pessoa mexer
  de novo (vale para todo formulário que usa ele). Dados da empresa: sem
  `configurar_empresa` a ficha é só leitura, com "Só o proprietário ou um
  administrador da conta edita". Conhecimento da IA: dois cartões; a chave já
  saía mascarada, e agora o cartão "Credencial do provedor" mostra o próprio
  resultado (salvar fechava o formulário e levava o "Salvo" junto). Horário:
  página vira "Horário e retomada", dois blocos do mesmo peso, cada um com o
  próprio "Salvo"; `/ajustes/retomada` redireciona para `#retomada`. Plano:
  "Seu plano e consumo" e "Solicitar alteração"; o pedido em aberto vem da
  auditoria (`ultimoAto`, novo em `repos/auditoria.ts`, com teste), porque
  não há tabela de pedido. `prints.mjs` ganhou `negocio contexto horario plano`.
  Nome do menu ("Horário de atendimento") fica para a 10.1.
- 23/09, tarefa 10.4 (Passos 1, 2 e 4): títulos "Criar seu acesso",
  "Criar sua empresa", "Cadastrar administrador da plataforma" (e "Cadastrar
  usuário da plataforma" quando já há gente), cada um dizendo o passo
  seguinte; o e2e `test/e2e/cadastro.ts` passou a procurar "Criar acesso".
  Erro e não encontrado: regra pura `voltaDoErro` (`src/core/volta-do-erro.ts`,
  com teste) usa o endereço (dentro de conta volta ao início dela; no próprio
  início vai para `/contas`; admin vai para `/admin/contas`); fora disso e nos
  `not-found` o link é a rota nova `/voltar`, que redireciona com
  `destinoAposEntrar`. `/f/[token]` vencido mostra a data, quem pedir e onde
  se gera outro, com "Entrar" e "Ver a ajuda" (`LinkAberto` ganhou `expiraEm`).
  **Passo 3 adiado**: o rodapé da barra lateral é o mesmo lugar do "Você" da
  7.5 (A2); fazer depois da Fase 7 na `main`, em cima do que ela deixar.
- 23/09, tarefa 10.5: `EscopoDoAdmin` (linha acima do título) nas cinco telas
  admin: "Contas de clientes", "Usuários da plataforma · papel global",
  "Registro de auditoria da plataforma", consumo e alertas. Usuários diz que o
  papel dali é o da plataforma e o de cada conta muda na conta. Sem print: a
  conta de revisão local não é admin da plataforma, e promovê-la mexeria na
  conta compartilhada. O "botão de suporte" do S10 é da 7.3 (A2).
