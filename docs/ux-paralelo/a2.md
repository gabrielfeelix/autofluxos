# A2: Fase 7, tarefa 5.9 e Fase 8

Branch `ux/a2-acesso-inbox`, pasta `../autofluxos-a2`, porta 3102.

## Onde parei

Fase 7 mergeada na `main` (7.1 a 7.5). Próximo: tarefa 5.9 (anotações no Inbox), depois Fase 8 (8.1 a 8.6).

Falta da Fase 7:
- 7.5 passo 6: e2e Playwright do membro de atendimento (trocar nome e foto pelo rodapé, menu sem Configurações). Conferido à mão com `.ux-local/editar-nome.mjs`, não virou teste versionado.
- Tirar da conta de revisão a pessoa `atende.a2@local.test` (criada para a 7.2/7.5) quando não precisar mais; o suporte `suporte.a2@local.test` não é membro.
- Achado para o A3 (Fase 9): o Início mostra atalho "Automações ›" para quem não tem acesso.

## Pendente para produção

- `supabase/migrations/0094_avatares.sql` (bucket público `autofluxos-avatares`, 2 MB, jpg/png/webp). Aplicada só no local. Sem ela, trocar foto em produção falha com "não deu para guardar a foto"; nome e senha funcionam.

## Registro

| Data | Tarefa | Commit | Observação |
|---|---|---|---|
| 23/09 | 7.1 | (este) | `resumoDoAcesso` e `ROTULO_DO_PAPEL` em `src/core/permissoes.ts`. Perfil calculado pela política efetiva (reabrir mantém o nome). Editor: perfis em cima, matriz em "Ajustes avançados" (abre sozinho se personalizado). Corrigido de brinde: o editor montava com `membro = null` e abria com as exceções vazias; agora monta só aberto. Nomes novos também em `/contas` e na área admin. |
| 23/09 | 7.2 | (este) | `EXIGENCIA_DA_SECAO` + `liberaSecao` em `secoes-do-cliente.tsx`; a moldura (`ClienteShell`) troca o conteúdo por `SemAcesso` quando a seção não é liberada, e por `FunilDesligado` com o CRM desligado (antes `/quadros` abria normal). `exigirCapacidadeNaPagina` (404) virou `capacidadeNaPagina` (null) e as páginas mostram `SemAcesso`. Editor de fluxo e Importar contatos ganharam a porta que faltava. Grupo da barra sem item some. Pessoa de teste: `atende.a2@local.test` / `senha-local-123456`, acesso de atendimento, na conta de revisão (tirar ao fim da Fase 7). |
| 23/09 | 7.2 E12 | (este) | Rotas com acesso de atendimento: abrem `/`, `/inbox`, `/atividades`, `/leads`, ficha, `/quadros`, `/favoritas`. `SemAcesso`: `/leads/segmentos`, `/leads/importar`, `/fluxos`, `/fluxos/<id>`, `/respostas`, `/transmissoes`, `/ajustes` e todas as subpáginas, `/configurar`. Achado para o A3 (Fase 9): o Início mostra atalho "Automações ›" para quem não tem acesso. |
| 23/09 | 7.3 | (este) | `FaixaDeSuporte` na moldura e no editor de fluxo; some quando a sessão é "entrar como" (a faixa de impersonação já fala). "Sair da conta" leva a `/painel`. Suporte local: `suporte.a2@local.test` / `senha-local-123456` (`role = admin`, sem membro). |
| 23/09 | 7.4 | (este) | `removerComDestino` (`repos/usuarios.ts`) numa transação: conversas, negociações abertas e atividades abertas vão para o destino (pessoa desta conta, conferida no servidor) ou ficam sem responsável; venda ganha e atividade concluída ficam com quem fez. `removerDaConta` apagado (só este caminho existe). `pendenciasDoMembro` conta atividades. Item do menu de Configurações e cartão do hub viraram "Pessoas e acesso" (o A3 reorganiza na 10.1). |
| 23/09 | 7.5 | (este) | Rodapé mostra a pessoa (foto ou iniciais, nome, estado); a conta subiu para o topo. Diálogo "Você" com Editar perfil, Trocar senha, presença, avisos, tema, Configurações da conta (só se `liberaSecao('ajustes')`), Trocar de conta, Sair. Papel mostrado é o perfil do resumo ("Acesso de atendimento · Conta"). Nome e foto otimistas por contexto (`PerfilDaSessao`). Foto recortada quadrada 256 px webp no navegador. Ações em `acoes-perfil.ts` só usam `sessaoAtual()`. Teste de integração feito; **e2e do passo 6 não escrito** (conferido com script local: atendente renomeia pelo rodapé e o menu não tem Configurações). |
| 23/09 | merge Fase 7 | (este) | Rebase sobre A1 (editor 5.3 a 5.6) e A4 (Relatórios). Conflito em `barra-lateral.tsx`: mantido o `relatorios` do A4 no grupo Dia a dia e o filtro de grupo vazio. A página de Relatórios do A4 usava `exigirCapacidadeNaPagina` (removida): passou a `capacidadeNaPagina` + `SemAcesso`, e `relatorios` entrou em `EXIGENCIA_DA_SECAO` com a mesma porta (atender). |
| 23/09 | 5.9 | (este) | Sessão de fechamento (um agente só, worktree `../autofluxos-ux`, porta 3101). **Desvio: sem tabela nova.** Já existia o diário do contato (`eventos_do_contato`, tipo `nota`, com autor e hora, 0058) e o código explicava por que não criar segunda tabela; cada anotação vira um evento `nota`. Sem migration, nada pendente para produção. A nota única antiga (`contacts.notas`) aparece como a entrada mais antiga, "de antes do histórico, sem data", em vez de ser copiada (copiar inventaria autor e hora). `ProvedorDeAnotacoes` (`components/inbox/anotacoes.tsx`) liga a entrada "+ Anotar" da barra de ações à lista da coluna do contato: otimista, "guardando…", erro com "Tentar de novo". `acaoAnotar` devolve a anotação gravada e não revalida o Inbox. A mesma lista na ficha (substitui `NotasDoContato`) e no painel do contato do Funil. Provado no navegador: duas anotações, zero recargas (evento `load`), aparecem na ficha. Achados: (1) o cabeçalho da conversa em 1440 px espremia o nome a nada e os ícones passavam por baixo da coluna do contato, sem clique; agora quebra linha. (2) Inbox no celular (390 px) mostra lista e conversa lado a lado, cortando a conversa; já era assim antes, fica para a Fase 8. |
