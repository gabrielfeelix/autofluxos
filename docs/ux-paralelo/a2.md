# A2: Fase 7, tarefa 5.9 e Fase 8

Branch `ux/a2-acesso-inbox`, pasta `../autofluxos-a2`, porta 3102.

## Onde parei

Fase 7 mergeada na `main` (7.1 a 7.5). Próximo: tarefa 5.9 (anotações no Inbox), depois Fase 8 (8.1 a 8.6).

Falta da Fase 7:
- 7.5 passo 6: e2e Playwright do membro de atendimento (trocar nome e foto pelo rodapé, menu sem Configurações). Conferido à mão com `.ux-local/editar-nome.mjs`, não virou teste versionado.
- Tirar da conta de revisão a pessoa `atende.a2@local.test` (criada para a 7.2/7.5) quando não precisar mais; o suporte `suporte.a2@local.test` não é membro.
- Achado para o A3 (Fase 9): o Início mostra atalho "Automações ›" para quem não tem acesso.

## Pendente para produção

- `supabase/migrations/0094_avatares.sql` (bucket público `autofluxos-avatares`, 2 MB, jpg/png/webp). Aplicada só no local. Sem ela, trocar foto em produção falha com "não deu para guardar a foto"; nome e senha funcionam.

## Registro

| Data | Tarefa | Commit | Observação |
|---|---|---|---|
| 23/09 | 7.1 | (este) | `resumoDoAcesso` e `ROTULO_DO_PAPEL` em `src/core/permissoes.ts`. Perfil calculado pela política efetiva (reabrir mantém o nome). Editor: perfis em cima, matriz em "Ajustes avançados" (abre sozinho se personalizado). Corrigido de brinde: o editor montava com `membro = null` e abria com as exceções vazias; agora monta só aberto. Nomes novos também em `/contas` e na área admin. |
| 23/09 | 7.2 | (este) | `EXIGENCIA_DA_SECAO` + `liberaSecao` em `secoes-do-cliente.tsx`; a moldura (`ClienteShell`) troca o conteúdo por `SemAcesso` quando a seção não é liberada, e por `FunilDesligado` com o CRM desligado (antes `/quadros` abria normal). `exigirCapacidadeNaPagina` (404) virou `capacidadeNaPagina` (null) e as páginas mostram `SemAcesso`. Editor de fluxo e Importar contatos ganharam a porta que faltava. Grupo da barra sem item some. Pessoa de teste: `atende.a2@local.test` / `senha-local-123456`, acesso de atendimento, na conta de revisão (tirar ao fim da Fase 7). |
| 23/09 | 7.2 E12 | (este) | Rotas com acesso de atendimento: abrem `/`, `/inbox`, `/atividades`, `/leads`, ficha, `/quadros`, `/favoritas`. `SemAcesso`: `/leads/segmentos`, `/leads/importar`, `/fluxos`, `/fluxos/<id>`, `/respostas`, `/transmissoes`, `/ajustes` e todas as subpáginas, `/configurar`. Achado para o A3 (Fase 9): o Início mostra atalho "Automações ›" para quem não tem acesso. |
| 23/09 | 7.3 | (este) | `FaixaDeSuporte` na moldura e no editor de fluxo; some quando a sessão é "entrar como" (a faixa de impersonação já fala). "Sair da conta" leva a `/painel`. Suporte local: `suporte.a2@local.test` / `senha-local-123456` (`role = admin`, sem membro). |
| 23/09 | 7.4 | (este) | `removerComDestino` (`repos/usuarios.ts`) numa transação: conversas, negociações abertas e atividades abertas vão para o destino (pessoa desta conta, conferida no servidor) ou ficam sem responsável; venda ganha e atividade concluída ficam com quem fez. `removerDaConta` apagado (só este caminho existe). `pendenciasDoMembro` conta atividades. Item do menu de Configurações e cartão do hub viraram "Pessoas e acesso" (o A3 reorganiza na 10.1). |
| 23/09 | 7.5 | (este) | Rodapé mostra a pessoa (foto ou iniciais, nome, estado); a conta subiu para o topo. Diálogo "Você" com Editar perfil, Trocar senha, presença, avisos, tema, Configurações da conta (só se `liberaSecao('ajustes')`), Trocar de conta, Sair. Papel mostrado é o perfil do resumo ("Acesso de atendimento · Conta"). Nome e foto otimistas por contexto (`PerfilDaSessao`). Foto recortada quadrada 256 px webp no navegador. Ações em `acoes-perfil.ts` só usam `sessaoAtual()`. Teste de integração feito; **e2e do passo 6 não escrito** (conferido com script local: atendente renomeia pelo rodapé e o menu não tem Configurações). |
| 23/09 | merge Fase 7 | (este) | Rebase sobre A1 (editor 5.3 a 5.6) e A4 (Relatórios). Conflito em `barra-lateral.tsx`: mantido o `relatorios` do A4 no grupo Dia a dia e o filtro de grupo vazio. A página de Relatórios do A4 usava `exigirCapacidadeNaPagina` (removida): passou a `capacidadeNaPagina` + `SemAcesso`, e `relatorios` entrou em `EXIGENCIA_DA_SECAO` com a mesma porta (atender). |
| 23/09 | 5.9 | (este) | Sessão de fechamento (um agente só, worktree `../autofluxos-ux`, porta 3101). **Desvio: sem tabela nova.** Já existia o diário do contato (`eventos_do_contato`, tipo `nota`, com autor e hora, 0058) e o código explicava por que não criar segunda tabela; cada anotação vira um evento `nota`. Sem migration, nada pendente para produção. A nota única antiga (`contacts.notas`) aparece como a entrada mais antiga, "de antes do histórico, sem data", em vez de ser copiada (copiar inventaria autor e hora). `ProvedorDeAnotacoes` (`components/inbox/anotacoes.tsx`) liga a entrada "+ Anotar" da barra de ações à lista da coluna do contato: otimista, "guardando…", erro com "Tentar de novo". `acaoAnotar` devolve a anotação gravada e não revalida o Inbox. A mesma lista na ficha (substitui `NotasDoContato`) e no painel do contato do Funil. Provado no navegador: duas anotações, zero recargas (evento `load`), aparecem na ficha. Achados: (1) o cabeçalho da conversa em 1440 px espremia o nome a nada e os ícones passavam por baixo da coluna do contato, sem clique; agora quebra linha. (2) Inbox no celular (390 px) mostra lista e conversa lado a lado, cortando a conversa; já era assim antes, fica para a Fase 8. |
- 23/09, tarefa 8.1 (sessão única, ex-A2): `estadoDoAtendimento` em
  `core/estado-do-atendimento.ts` (6 testes) junta handoff, pausa, sessão com
  pessoa (`sessaoComPessoa`, nova leitura de uma linha) e conversa resolvida
  em quatro estados com dono, efeito e próxima ação. `SeloDoAtendimento` e
  `CartaoDoAtendimento` (`components/atendimento/estado.tsx`) no cabeçalho e
  na coluna do Inbox e na ficha; a ficha perdeu as duas faixas separadas.
  Finalizar confirma "Na próxima mensagem o bot volta a responder". Achado:
  o cabeçalho dizia "robô pausado" só por haver responsável, e responsável
  não cala o bot (quem cala é a sessão `humano`); com isso, a coluna ao lado
  dizia "BOT RESPONDENDO" na mesma conversa. O botão "Atendimento finalizado"
  virou "Finalizar atendimento" (textos que citavam o nome antigo
  atualizados). Na fila, a insígnia diz "não lidas para você" e a linha do
  dono virou "responsável: Fulano" / "responsável: você"; abrir só marca
  leitura (conferido no código: `marcarComoLida`, nada de atribuir).
- 23/09, tarefa 8.2: um compositor só com três modos. **Livre** (dentro da
  janela), **modelo** (fora dela, com ids: o `RetomarComModelo` entra dentro
  do compositor, acima do campo, com prévia "Para Fulano:" do corpo com as
  variáveis trocadas pelo nome) e **bloqueado** (sem ids). O campo não some
  mais: fica desabilitado com o motivo e o rascunho. Rascunho guardado em
  `sessionStorage` por conversa (`af:rascunho:<contato>`), restaurado pelo
  setter nativo + evento `input` (o lint recusa setState no efeito). A Ficha
  passou a mandar `conversa` e ganhou o modo modelo. Respostas rápidas
  saíram da fileira de chips para `SeletorDeRespostaRapida`, botão da barra
  irmão do de produto: `/` no campo vazio abre, setas, Enter insere no
  cursor, Esc fecha e devolve o foco, anúncio em `aria-live`. Conferido com
  `.ux-local/compositor.mjs` (valor inserido, foco no campo, rascunho volta
  depois de trocar de conversa). **Seed local:** o modelo `lembrete_de_aula`
  está gravado no formato da Meta (lista), o app lê `corpo` vazio e a prévia
  não aparece; em conta real o formato é o do app.
- 23/09, tarefa 8.3: ações do Inbox em dois grupos com moldura e dica
  "Para a equipe: ..." (adiar, marcar atividade, anotar) e "Para o contato:
  agendar mensagem"; o painel aberto repete o destino no alto. A ficha usa os
  mesmos grupos (Atividade, Anotar, Etiquetar | Agendar). **Desvio:** anotar
  entrou em "Para a equipe" junto de adiar e atividade (só a equipe vê). Aba
  Atividades com três cartões e selo "Quem faz" (pessoa da equipe / o sistema,
  na hora marcada / uma sequência automática), com vazio escrito em cada um; a
  contagem da aba soma os pendentes dos três e a dica diz quanto de cada.
  Resumo no topo da ficha ganhou a segunda linha: última mensagem da pessoa,
  próxima atividade e mensagem agendada (cada uma leva à aba). "Fixo" ficou
  como "acima das abas, visível em todas"; sticky com dois dropdowns comeria o
  celular. `prazoEmPalavras` foi para `core/atividades.ts` e passou a contar por
  dia quando não há hora marcada (antes "em 21 h" para uma proposta de amanhã).
  `horaComFuso` em `lib/quando.ts`: mensagem agendada mostra "(Brasília)".
- 23/09, tarefa 8.4: `FecharCartao` lista "O que acontece" e "O que não
  muda" no lugar da frase única. Os efeitos vêm do código, não do plano:
  ganhar pelo modal simples **não grava venda** (só marca o cartão com o
  valor; venda é o `RegistrarVenda` do funil comercial), então o texto diz
  "marcado como ganho, com o valor", sem "venda registrada". O funil seguinte
  aparece pelo nome no quadro (`seguinte` vem de `seguinteId`); na ficha, que
  não sabe o destino, a frase fala da regra. "O que não muda": conversa e
  bot, outros negócios, atividades abertas (e, ao perder, ninguém é
  apagado). `RegistrarVenda` ganhou a frase da conversa. Segmento sem
  condição mostra o selo "Todos os contatos" no editor (no lugar da junção,
  que não se aplica) e na lista. Prévia guarda a regra que a gerou; mudou
  depois, fica apagada com "A regra mudou depois desta prévia". `porNoQuadro`
  e `porNaEtapa` devolvem `jaEstavam` e `falharam`; a seleção de Contatos diz
  entraram / já estavam (com "ver no funil") / não entraram. "Trazer todos"
  não mudou: só pega quem está fora.
- 23/09, tarefa 8.5: `core/volta-da-ficha.ts` (`abaDaFicha`, `voltaDaFicha`,
  `hrefDaFicha`, testes). A ficha lê `?aba=` (foca a aba ao chegar) e
  `?volta=` (só endereço desta conta: site de fora, `//`, barra invertida e
  outra conta caem em "← Contatos"); o botão diz para onde volta ("← Inbox",
  "← Atividades", "← Funil"...). Mandam `volta`: Inbox ("Ver ficha
  completa"), agenda (linha e menu, abrindo em Atividades), painel do
  funil, detalhe da transmissão (abrindo na Conversa) e Início. Aviso na
  lista do Inbox quando a conversa aberta não está nela: "fora do filtro
  atual" no modo local, "não está nesta página" no paginado; fica na lista
  (cliente) para sumir sozinho ao trocar de filtro. `server/recarregar-
  contato.ts` substitui os `revalidatePath` soltos de encerrar, bot,
  nome, adiar, resolver, assumir, liberar, atribuir, agendar e atividades
  (antes adiar e resolver só revalidavam o Inbox). E2e novo
  `test/e2e/contexto.spec.ts`: resolver no Inbox aparece na ficha, trocar o
  responsável na ficha aparece no Inbox, com uma carga de página só; e a aba
  pedida abre focada. **Desvio:** o plano pedia "concluir atividade na ficha
  e voltar ao Inbox", mas o Inbox não mostra atividade; o teste usa
  responsável e estado, que as duas telas mostram. **Inbox no celular**: a
  outra sessão (`0deeb1c`, plano da casca) já pôs uma coluna por vez;
  conferido no print de 390 px.
