# A2: Fase 7, tarefa 5.9 e Fase 8

Branch `ux/a2-acesso-inbox`, pasta `../autofluxos-a2`, porta 3102.

## Onde parei

Fase 7 em andamento. Feito: 7.1, 7.2.

## Pendente para produção

Nada ainda.

## Registro

| Data | Tarefa | Commit | Observação |
|---|---|---|---|
| 23/09 | 7.1 | (este) | `resumoDoAcesso` e `ROTULO_DO_PAPEL` em `src/core/permissoes.ts`. Perfil calculado pela política efetiva (reabrir mantém o nome). Editor: perfis em cima, matriz em "Ajustes avançados" (abre sozinho se personalizado). Corrigido de brinde: o editor montava com `membro = null` e abria com as exceções vazias; agora monta só aberto. Nomes novos também em `/contas` e na área admin. |
| 23/09 | 7.2 | (este) | `EXIGENCIA_DA_SECAO` + `liberaSecao` em `secoes-do-cliente.tsx`; a moldura (`ClienteShell`) troca o conteúdo por `SemAcesso` quando a seção não é liberada, e por `FunilDesligado` com o CRM desligado (antes `/quadros` abria normal). `exigirCapacidadeNaPagina` (404) virou `capacidadeNaPagina` (null) e as páginas mostram `SemAcesso`. Editor de fluxo e Importar contatos ganharam a porta que faltava. Grupo da barra sem item some. Pessoa de teste: `atende.a2@local.test` / `senha-local-123456`, acesso de atendimento, na conta de revisão (tirar ao fim da Fase 7). |
| 23/09 | 7.2 E12 | (este) | Rotas com acesso de atendimento: abrem `/`, `/inbox`, `/atividades`, `/leads`, ficha, `/quadros`, `/favoritas`. `SemAcesso`: `/leads/segmentos`, `/leads/importar`, `/fluxos`, `/fluxos/<id>`, `/respostas`, `/transmissoes`, `/ajustes` e todas as subpáginas, `/configurar`. Achado para o A3 (Fase 9): o Início mostra atalho "Automações ›" para quem não tem acesso. |
