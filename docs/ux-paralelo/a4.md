# A4: registro e handoff

## Onde parei

Fase 11 (11.1) feita e mergeada na `main`. Próximo: esperar as Fases 5 a 10
na `main` para começar a 12; enquanto isso, revisar os merges dos outros e
anotar achados aqui embaixo.

Ambiente: `../autofluxos-a4`, `PORTA=3104`. Conta com movimento para
Relatórios: `npx tsx scripts/ux-local/seed-relatorios.mts` cria "Studio
Relatórios Local" (dono `revisao@local.test`, Ana/Bruno/Carla membros, 120 dias
de conversas, filas, notas e fechamentos); `--apagar` remove. Prints dela:
`CONTA="Studio Relatórios Local" node scripts/ux-local/prints.mjs <saída> relatorios`.

## Registro

- 23/09, 11.1, passo 1, as métricas que existem (`src/server/repos/metricas.ts`
  e as views): **desfechos** (`metricas_de_desfecho`, 0086: conversa por mês
  de início, fatias bot/prevista/falha/aberta, o primeiro handoff decide);
  **tempos** (`metricas_de_tempo`, 0028: do handoff até a primeira saída e até
  `resolvido_em`, mediana e média); **série diária** (`metricas_diarias`, 0028:
  contatos novos por `contacts.criado_em`, conversas por sessão iniciada,
  idas para pessoa por handoff criado); **por pessoa** (`metricas_por_pessoa`:
  handoffs por `contacts.atribuido_a`); **satisfação** (`metricas_de_satisfacao`,
  0060, NPS com a faixa de `core/nps.ts`); **funil do mês** (`metricas_sessoes`,
  usado só na lista de automações). Fora do arquivo, mas já na home:
  **fechamentos** (`painel.ts`, cartões ganhos/perdidos por `fechado_em`).
  Todas no dia de São Paulo.
- 23/09, 11.1, desvio: as views são **mensais**; período de 7 dias não se soma
  de meses e mediana não se soma de nada. A conta por período roda em SQL
  parametrizado (`src/server/repos/relatorios.ts`) com as mesmas definições,
  pelo pool que já fala Postgres (`bancoDoLogin`). **Sem migration**, nada a
  aplicar em produção.
- 23/09, 11.1, desvio: não existe capacidade "ler métricas". A tela e o item de
  menu seguem `atender` (todo papel tem): o corte é na consulta, pelo escopo
  (`tudo` = conta inteira, `equipes`, `proprios` = só contatos com a pessoa).
  Quando a Fase 7 (menu por capacidade) chegar, o item entra na regra dela.
- 23/09, 11.1, passo 6: saíram da home "Este mês" (desfechos e espera),
  Fechamentos, Satisfação e Quem atendeu; entrou o link "Ver relatórios".
  **Clientes sumindo ficou**: é pendência com nome e link para agir, não número
  de período. `comoDuracao` foi para `core/relatorios.ts`. `medirDesfechos`,
  `medirTempos` e `medirPessoas` ficaram sem uso na tela (só nos testes).
- 23/09, 11.1, `5dd3ea8` tela e `feat(relatorios): números do período saem da
  home`. Testes: `src/core/relatorios.test.ts` (18, unit) e
  `src/server/repos/relatorios.test.ts` (4, integração, inclui "relatório de
  quem vê só os próprios não soma a conta inteira" e a conversa das 22h30).
